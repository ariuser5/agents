# Legacy Entrypoint

This file remains only for agents configured before `AGENTS.md` became the
shared entrypoint. Read and follow the sibling `AGENTS.md` as the sole canonical
startup instruction, then use its catalog for routing.

Do not treat this file as a second policy layer. Update the agent's native
adapter to point directly to `~/.agents/AGENTS.md` when practical.
