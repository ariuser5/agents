# Agent Setup Notes

The bootstrap only copies this rules bundle to `~/.agents`. It does not modify
agent configuration. Use the relevant note in this directory to register
`~/.agents/global.md` with each agent you use.

All notes use the same instruction: load `global.md` always, resolve referenced
paths from `~/.agents`, and read detailed resources only when routed.
